[project]
name = "comfyui-zerotrain-slider"
description = "Concept-slider LoRAs solved in closed form from the model's own text prior, then optionally refined by warm-started gradient training. Krea 2 support included; other models are one file each."
version = "3.0.1"
license = { text = "GPL-3.0-or-later" }
dependencies = []

[tool.comfy]
PublisherId = ""
DisplayName = "Zero-Training Slider LoRA"
Icon = ""
